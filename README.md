# Habit Tracker Pro

Local Flask habit tracker using JSON storage only.

## Setup

1. Create and activate a Python virtual environment.
2. Install dependencies:

```bash
pip install Flask pytest
```

## Run the app

```bash
python app.py
```

Open `http://127.0.0.1:5000` in your browser.

## Run tests

```bash
pytest
```
